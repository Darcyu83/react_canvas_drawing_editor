import styled from '@emotion/styled'

export const MainLayoutContainer = styled.div`
  padding: 0 8px;
  height: 100dvh;

  display: flex;
  flex-direction: column;
  gap: 24px;

  & > main {
    height: 100%;
  }
`
